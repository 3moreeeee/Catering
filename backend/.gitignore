# Real secrets. .env.example carries the documented, valueless template.
.env

# Build output
target/
out/

# IDE
.idea/
*.iml

# Local H2 fallback database, used only when no Neon URL is configured.
data/
